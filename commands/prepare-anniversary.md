---
description: Prepare the RSoW or BBtn closing community and signature paragraph.
---

Use the `newsletter-anniversary` skill in `skills/07-anniversary/SKILL.md`.

Prepare, update, or finalize the closing community and signature paragraph for the current issue. If `$ARGUMENTS` specifies a newsletter, issue, publication date, source note, or constraint, honor it. Otherwise infer the publication date and target issue from the Obsidian vault structure.

At the end, summarize the publication date, selected closing note, source used, created or updated files, approval state, and editor-attention points.
