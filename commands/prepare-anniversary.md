---
description: Prepare the RSoW or BBtn closing anniversary paragraph.
---

Use the `newsletter-anniversary` skill in `skills/07-anniversary/SKILL.md`.

Prepare, update, or finalize the closing anniversary paragraph for the current issue. If `$ARGUMENTS` specifies a newsletter, issue, publication date, source note, or constraint, honor it. Otherwise infer the publication date and target issue from the Obsidian vault structure.

At the end, summarize the publication date, selected anniversary, source used, rejected notable negative candidates if relevant, created or updated files, approval state, and editor-attention points.
