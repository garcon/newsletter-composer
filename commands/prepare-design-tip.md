---
description: Prepare the RSoW Designérský tip or BBtn Designer’s Tip section from text and image inputs.
---

Use the `newsletter-design-tip` skill in `skills/02-design-tip/SKILL.md`.

Prepare, update, or finalize the design tip section for the current issue. If `$ARGUMENTS` specifies a newsletter, issue, target folder, or constraint, honor it. Otherwise infer the target issue from the Obsidian vault structure.

Handle image assets according to the skill, including saving images into `assets` and creating missing alt text when possible. At the end, summarize used sources, image handling, moved files, created or updated files, approval state if relevant, and remaining editor-attention points.
