---
description: Research and prepare the RSoW and BBtn Opinion essays from an inbox topic.
---

Use the `newsletter-opinion` skill in `skills/03-opinion/SKILL.md`.

Prepare the Opinion workflow from the inbox topic. Unless `$ARGUMENTS` asks for only one newsletter, create both BBtn and RSoW working outputs. Honor any issue, newsletter, topic, length, or editorial constraint in `$ARGUMENTS`.

At the end, summarize the topic, key research sources, created files, approval state, and editor-attention points.
